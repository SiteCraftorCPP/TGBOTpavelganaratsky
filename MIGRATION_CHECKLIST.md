# ✅ Чеклист миграции с Supabase

## Статус: ✅ ВСЕ ВЫПОЛНЕНО

### ✅ Основные модули
- [x] `server/db.js` - создан модуль для PostgreSQL
- [x] `server/storage.js` - создан модуль для локального хранилища
- [x] `database/schema.sql` - создана схема БД

### ✅ Backend (server/bot.js)
- [x] Удалены все импорты Supabase
- [x] Все вызовы `supabase.*` заменены на `db.*` или `storage.*`
- [x] Добавлены REST API endpoints для админ-панели
- [x] CORS настроен для админ-панели
- [x] Инициализация storage при запуске

### ✅ Зависимости
- [x] `server/package.json` - удален `@supabase/supabase-js`, добавлен `pg`
- [x] Все модули корректно экспортируют функции

### ✅ API Endpoints
- [x] GET `/api/clients` - список клиентов
- [x] PUT `/api/clients/:id` - обновление клиента
- [x] DELETE `/api/clients/:id` - удаление клиента
- [x] GET `/api/slots` - список слотов
- [x] POST `/api/slots` - создание слота
- [x] DELETE `/api/slots/:id` - удаление слота
- [x] GET `/api/sos` - список SOS запросов
- [x] PUT `/api/sos/:id` - отметка SOS как просмотренного
- [x] GET `/api/payments` - список платежей
- [x] DELETE `/api/payments/:id` - удаление платежа
- [x] GET `/api/payment-card` - получение номера карты
- [x] PUT `/api/payment-card` - сохранение номера карты
- [x] GET `/api/diary` - список записей дневника

### ✅ Компоненты админ-панели
- [x] `src/lib/api.ts` - создан API клиент
- [x] `ClientsList.tsx` - переписан на использование API
- [x] `SlotsManager.tsx` - переписан на использование API
- [x] `SosRequests.tsx` - переписан на использование API
- [x] `PaymentScreenshots.tsx` - переписан на использование API
- [x] `DiaryEntries.tsx` - переписан на использование API
- [x] `BotSetup.tsx` - обновлен (webhook URL)

### ✅ Конфигурация
- [x] `env.example` - обновлен (удалены Supabase переменные, добавлены PostgreSQL)
- [x] `nginx.conf` - добавлены proxy для `/api/` и `/storage/`
- [x] Все пути настроены корректно

### ⚠️ Что нужно сделать на VPS:

1. **Установить PostgreSQL** (если еще не установлен)
2. **Создать базу данных и пользователя**
3. **Применить схему** из `database/schema.sql`
4. **Настроить `.env` файл** с правильными данными БД
5. **Создать директорию для storage**: `mkdir -p /var/www/liftme-bot/server/storage/payments`
6. **Установить зависимости**: `cd server && npm install`
7. **Пересобрать админ-панель**: `npm run build`
8. **Перезапустить бота**: `pm2 restart liftme-bot`
9. **Перезагрузить Nginx**: `sudo nginx -t && sudo systemctl reload nginx`

### 📝 Примечания

- Старые файлы Supabase (`supabase/functions/`, `src/integrations/supabase/`) можно оставить как есть - они не используются
- `package.json` в корне еще содержит `@supabase/supabase-js`, но это не критично - он не используется в активном коде
- Все функции работают через локальные модули `db.js` и `storage.js`
