# Журнал изменений - 11 пунктов

## Краткое описание

Все 11 изменений внесены и готовы к деплою.

## Детали изменений

### 1. ✅ Кнопка рассылки в меню администратора
- **Файл**: `server/bot.js`
- **Изменения**:
  - Добавлена кнопка "📢 Рассылка" в меню администратора
  - Добавлена функция `handleBroadcast()` для инициации рассылки
  - Добавлена функция `sendBroadcast()` для отправки сообщений всем клиентам
  - Добавлен state `waiting_broadcast`
  - Обработка команды `/cancel` для отмены рассылки

### 2. ✅ Замена "lovable" на "Твой Ассистент"
- **Файл**: `src/pages/Index.tsx`
- **Изменения**: Надпись под логотипом изменена с "Панель управления" на "Твой Ассистент"

### 3. ✅ Исправление выступов в мобильной версии
- **Файл**: `src/pages/Index.tsx`
- **Изменения**: 
  - Добавлены классы `max-w-7xl` и `w-full` для header и main
  - Заменён `container mx-auto` на `w-full ... max-w-7xl mx-auto`

### 4. ✅ Исправление функции "записать клиента"
- **Файлы**: 
  - `server/bot.js` - добавлен endpoint `/book-for-client`
  - `nginx.conf` - добавлен proxy для `/book-for-client`
  - `src/components/admin/ClientsList.tsx` - обновлён для использования нового endpoint
- **Изменения**: Создан Node.js endpoint вместо Supabase Edge Function

### 5. ✅ Кнопка меню на главном экране
- **Файл**: `server/bot.js`
- **Изменения**:
  - Добавлена reply keyboard с кнопкой "📋 Меню"
  - Обновлена функция `sendMessage()` для автоматического добавления reply keyboard
  - Обработчик команды "📋 Меню" добавлен

### 6. ✅ Удаление картинки из меню
- **Файл**: `server/bot.js`
- **Изменения**: 
  - Заменён `sendPhoto()` на `sendMessage()` в `handleMainMenu()`
  - Удалена загрузка `menu-image.jpg`

### 7. ✅ Автоматическое закрытие календаря
- **Файл**: `src/components/admin/SlotsManager.tsx`
- **Изменения**:
  - Добавлен state `calendarOpen`
  - Календарь автоматически закрывается после выбора даты: `setCalendarOpen(false)`

### 8. ✅ Кнопка "Написать" клиенту
- **Файл**: `src/components/admin/ClientsList.tsx`
- **Изменения**: Добавлена кнопка с иконкой MessageCircle, открывающая диалог в Telegram

### 9. ✅ Расписание с 7:00 до 22:00
- **Файл**: `src/components/admin/SlotsManager.tsx`
- **Изменения**: Изменён диапазон времени с `8-20` на `7-22`

### 10. ✅ Удаление старых записей через 48 часов
- **Файл**: `src/components/admin/SlotsManager.tsx`
- **Изменения**: Cutoff date изменён с `-1` день на `-2` дня (48 часов)

### 11. ✅ Исправление отправки скриншотов + автоудаление
- **Файлы**:
  - `server/bot.js` - добавлена отдельная обработка `update.message.photo` в webhook
  - `server/cleanup-old-payments.js` - создан скрипт для автоматической очистки
  - `supabase/migrations/20260109000000_create_payments_bucket.sql` - создание bucket
- **Изменения**:
  - Фото теперь обрабатываются отдельно от текстовых сообщений
  - Создан cron-скрипт для удаления скриншотов старше 7 дней

## Инструкции по деплою

### На сервере (VPS)

```bash
# 1. Перейдите в директорию проекта
cd /var/www/liftme-bot

# 2. Получите последние изменения
git pull

# 3. Установите зависимости (если нужно)
npm install
cd server && npm install && cd ..

# 4. Соберите фронтенд
npm run build

# 5. Обновите nginx конфигурацию
sudo cp nginx.conf /etc/nginx/sites-available/liftme-bot
sudo nginx -t
sudo systemctl reload nginx

# 6. Перезапустите бота
cd server
pm2 restart liftme-bot
pm2 logs liftme-bot --lines 20
```

### В Supabase Dashboard

1. Откройте SQL Editor
2. Выполните SQL из `supabase/migrations/20260109000000_create_payments_bucket.sql`:
   ```sql
   -- Create storage bucket for payment screenshots if it doesn't exist
   INSERT INTO storage.buckets (id, name, public)
   VALUES ('payments', 'payments', true)
   ON CONFLICT (id) DO NOTHING;

   -- Set up storage policies for payments bucket
   CREATE POLICY IF NOT EXISTS "Allow service role full access to payments"
   ON storage.objects FOR ALL
   TO service_role
   USING (bucket_id = 'payments');

   CREATE POLICY IF NOT EXISTS "Allow public read access to payments"
   ON storage.objects FOR SELECT
   TO public
   USING (bucket_id = 'payments');
   ```

### Настройка автоочистки платежей

```bash
# Откройте crontab
crontab -e

# Добавьте строку для запуска очистки каждый день в 2:00
0 2 * * * cd /var/www/liftme-bot/server && /usr/bin/node cleanup-old-payments.js >> /var/log/cleanup-payments.log 2>&1
```

## Тестирование

Запустите тестовый скрипт:
```bash
chmod +x test-bot-features.sh
./test-bot-features.sh
```

## Проверка функций

### В боте (Telegram)

1. **Рассылка** (только для админа 783321437):
   - Нажмите "📋 Меню"
   - Должна появиться кнопка "📢 Рассылка"
   - Отправьте тестовое сообщение

2. **Кнопка Меню**:
   - Внизу экрана должна быть кнопка "📋 Меню"
   - При нажатии открывается главное меню

3. **Без картинки**:
   - При открытии меню не должно быть изображения

4. **Оплата**:
   - Нажмите "💳 Оплата"
   - Отправьте скриншот
   - Должно прийти подтверждение

5. **Расписание**:
   - Проверьте свободные слоты - должны быть с 7:00 до 22:30

### В админ панели (https://liftme.by)

1. **Надпись**:
   - Под логотипом должно быть "Твой Ассистент"

2. **Мобильная версия**:
   - Откройте на телефоне
   - Вкладки "Клиенты" и "SOS" не должны выступать за края

3. **Календарь**:
   - Откройте "Расписание" → "Добавить слот"
   - Выберите дату - календарь должен автоматически закрыться

4. **Записать клиента**:
   - Откройте "Клиенты"
   - Нажмите "Назначить" на любом клиенте
   - Выберите дату и время - должно работать без ошибок

5. **Написать клиенту**:
   - Откройте "Клиенты"
   - Должна быть кнопка "Написать" рядом с "Назначить"
   - При нажатии открывается Telegram

6. **Расписание до 22:00**:
   - В списке времени должны быть слоты до 22:30

7. **Старые записи**:
   - Записи исчезают из списка через 48 часов после даты слота

8. **Скриншоты оплаты**:
   - Вкладка "Оплаты"
   - Скриншоты хранятся 7 дней, затем автоматически удаляются

## Возможные проблемы

### Ошибка "Could not find the table"
- Выполните `NOTIFY pgrst, 'reload schema';` в Supabase SQL Editor

### Бот не отвечает
- Проверьте логи: `pm2 logs liftme-bot`
- Перезапустите: `pm2 restart liftme-bot`

### Nginx ошибка 502
- Проверьте, что бот запущен: `pm2 status`
- Проверьте nginx: `sudo nginx -t`

### Скриншоты не загружаются
- Проверьте, что bucket создан в Supabase Storage
- Проверьте policies для bucket

## Статус

- ✅ Все 11 пунктов реализованы
- ✅ Код проверен (no lint errors)
- ✅ Готово к деплою
- ✅ Документация создана
- ✅ Тестовый скрипт создан
